# Copy to .env for local runs (never commit real .env).
# PORT is provided automatically by most hosts (Render/Azure).
PORT=3000
# Optional: require a shared access code before anyone can enter. Leave blank for open access.
TRIVIA_PASSCODE=
# Optional: only needed to run multiple instances (scale-out).
REDIS_URL=
